interface:
  display_name: "ELI10 Decision Briefs"
  short_description: "Format user decisions with plain-English stakes, recommendations, pros/cons, and a Net line"
  default_prompt: "Use $eli10 to format this user-facing decision question with ELI10 context, concrete stakes, a recommendation, honest pros/cons, and a closing Net line."

policy:
  allow_implicit_invocation: true
